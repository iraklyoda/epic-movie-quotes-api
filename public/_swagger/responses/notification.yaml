components:
  responses:
    notifications:
      "200":
        description: OK
        content:
          application/json:
            schema:
              $ref: "../schemas/notification.yaml#/components/schemas/NotificationResponse"
